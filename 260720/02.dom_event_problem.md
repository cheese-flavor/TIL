### java script 전반적 구조###
```
1.  const 정의 우선
    const 사용할 명칭 = document.querySelector('#~')
    # 속성의 값을 가져오는거면 (ex: id 등) '#'을 꼭 쓰기
    # 일반적으로는 그냥 객체 이름 그대로

2. fuction 이름(event, ...) {
    가져온 값을 변수에 집어넣기

}

3. 
